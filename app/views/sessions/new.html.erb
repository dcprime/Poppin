<%= render 'shared/content_title', title: 'Welcome to Poppin!' %>

<ul>
<li>Poppin is an easy way to let your friends know that you're ready for them to pop in on you!</li>
  <br/>
<li>You must register if you want to try Poppin. If you already have an account, please sign in below.</li>
</ul>
<br/>

<div class='well'>
  <%= form_tag '/login' do %>
    <div class='form-group'>
      <%= label_tag :username %>
      <%= text_field_tag :username, params[:username] || '' %>
    </div>
    <div class='form-group'>
      <%= label_tag :password %>
      <%= password_field_tag :password, params[:password] || '' %>
    </div>
    <%= submit_tag "Login", class: 'btn btn-success' %>
  <% end %>
</div>